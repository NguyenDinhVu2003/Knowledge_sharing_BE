package com.company.knowledge_sharing_backend.entity;

public enum SharingLevel {
    PRIVATE,    // Only owner can access
    GROUP,      // Group members can access
    PUBLIC      // All authenticated users can access
}

